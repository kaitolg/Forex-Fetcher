{
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "electron": "NODE_ENV=development electron .",
    "electron:build": "npm run build && electron-builder -c.extraMetadata.main=main.js",
    "test": "jest"
  },
  "dependencies": {
    "electron-updater": "^5.3.0",
    "zustand": "^4.4.1",
    "react-error-boundary": "^4.0.11"
  },
  "devDependencies": {
    "electron": "^28.1.0",
    "electron-builder": "^24.9.1",
    "@types/lightweight-charts": "^3.8.2",
    "jest": "^29.7.0",
    "typescript": "^5.3.3"
  }
}